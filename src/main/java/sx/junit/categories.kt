package sx.junit

/**
 * Standard unit test
 */
interface StandardTest

/**
 * A category which can be applied on prototype tests, so they can be efficiently excluded from unit test runss
 */
interface PrototypeTest

