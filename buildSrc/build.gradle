apply plugin: 'java'

repositories {
    mavenCentral()
    jcenter()

    maven {
        url '../leoz-build/repo'
    }
}

dependencies {
    compile(
            "sx:sx-serialization:0.1",
            "sx:sx-packager-gradle:0.1"
    )
}